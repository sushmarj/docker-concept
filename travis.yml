sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t sushmajogi/docker-concept -f Dockerfile.dev .

script:
  - docker run sushmajogi/docker-concept npm run test -- --coverage
