sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t sushmajogi/docker-react -f Dockerfile.dev .

script:
  - docker run sushmajogi/docker-react npm run test -- --coverage
